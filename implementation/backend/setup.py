"""Setup script for the NeuraForge package."""

from setuptools import setup, find_packages

setup(
    name="neuraforge",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies are already installed in the virtual environment
    ],
    description="NeuraForge - Intelligent Multi-Agent System",
    author="NeuraForge Team",
)
